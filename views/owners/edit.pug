html
    head        
        title  Organizador
    body                    
        if owner.updateStatus
            p.red Registro actualizados correctamente        
        form(action=`/owners/${owner.id}?_method=PUT`, method="POST")                
            label Organizadores
            br
            input(type="text", name="nombre" value= owner.nombre)
            br            
            label Página web
            br
            input(type="url", name="website" value= owner.website)
            br            
            input(type="submit", value="Guardar")
        form(action=`/owners/${owner.id}?_method=DELETE`, method="POST")
            input(type="submit", value="Eliminar")
                        
        